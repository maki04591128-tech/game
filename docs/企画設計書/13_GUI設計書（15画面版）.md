# 13_GUI設計書（15画面版）

> **📌 本書について**
>
> 本書は新企画書 `docs/01_新企画書.md`（málfræðingur — Rúnagaldramaðurinn）を主軸とする新ドキュメント体系の一部です。
> 旧設計書は `docs/archive/企画設計書_旧/` に退避されています。
> **Phase A（2026-04-17）時点では TOC 骨子のみ**。本文は後続フェーズ（Phase C 以降）で肉付けします。

---

## 0. 目的

GUI 画面を 29 → 15 以下に削減する（新企画書 §10.1）。

## 1. 新企画書との対応

- 主な参照先：`docs/01_新企画書.md`

## 2. 流用・改修・新規の方針

- **流用**: `MainWindow`、`TitleWindow`、`SettingsWindow`、`StatusWindow`、`InventoryWindow`、`SpellCastingWindow`、`VocabularyWindow`、`DialogueWindow`、`WorldMapWindow`、`PauseWindow`、`SaveDataSelectWindow`、`CharacterCreationWindow`、`GameOverWindow`、`MessageLogWindow`（日記へ統合）、`KeyBindingWindow`。
- **削除**: `CompanionWindow`、`RecruitCompanionWindow`、`CraftingWindow`、`SkillTreeWindow`、`ShopWindow`（仕様縮小）、`ReligionWindow`、`TownWindow`、`TravelEventWindow`、`DifficultySelectWindow`、`DeathLogWindow`（日記へ統合）、`EncyclopediaWindow`（日記へ統合）、`QuestBoardWindow` / `QuestLogWindow`（日記へ統合検討）。

## 3. 目次（後続フェーズで肉付け）

1. 採用 15 画面の一覧と責務
2. 廃止画面と統合先
3. 画面遷移図（旧 20_GUI画面遷移図.md を簡略化）
4. キーバインド再整理

---

## 更新履歴

| 日付 | 更新内容 |
|------|---------|
| 2026-04-17 | Phase A：TOC 骨子のみの雛形を作成 |
